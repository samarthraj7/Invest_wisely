fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
SQLAlchemy==2.0.36

# Deck parsing
PyMuPDF==1.24.13
python-pptx==1.0.2

# Clients
anthropic==0.39.0
exa-py==1.7.1
requests==2.32.3

# Report export
jinja2==3.1.4
python-docx==1.1.2

# Optional: native PDF export. WeasyPrint needs system GTK/Pango/Cairo libs and
# has no Windows wheel, so it's NOT installed by default. HTML + DOCX export and
# browser "Print -> Save as PDF" work without it. To enable server-side PDF:
#   pip install -r requirements-optional.txt
# (see requirements-optional.txt)
